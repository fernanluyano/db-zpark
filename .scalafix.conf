rules = [
  RemoveUnused,
  DisableSyntax
]

# Configure RemoveUnused to handle more than just imports
RemoveUnused {
  imports = true
  privates = true      # Remove unused private members
  locals = true        # Remove unused local variables
  patternvars = true   # Remove unused pattern variables
  params = false       # Usually keep params for interface compatibility
}